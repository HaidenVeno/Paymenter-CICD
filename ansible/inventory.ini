# The self-hosted runner IS the homelab deployment host, so Ansible runs
# against localhost with a local connection. Point this elsewhere if you split
# the runner and the deploy target onto different machines.
[homelab]
localhost ansible_connection=local
